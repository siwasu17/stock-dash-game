# Phase 2: チャートコンポーネント 実装計画

## 1. チャート基盤の構築
- [ ] `chart.js` と `chartjs-plugin-streaming` の初期設定。
- [ ] リアルタイムデータ描画用のカスタムコンポーネント `RealtimeChart.tsx` の作成。
- [ ] タイムスケールアダプター（`chartjs-adapter-date-fns` 等）の導入。

## 2. デザインとレイアウト
- [ ] shadcn/ui の `Card` コンポーネントの導入（手動またはCLI）。
- [ ] 5つの銘柄を 2x3 または 1+4 のグリッド形式で表示。
- [ ] チャートの見た目のカスタマイズ（ダークモード対応、グリッド線の調整）。

## 3. データ同期
- [ ] `useGameStore` の価格更新をチャートにストリーミング。
- [ ] チャート内での描画負荷の最適化。
